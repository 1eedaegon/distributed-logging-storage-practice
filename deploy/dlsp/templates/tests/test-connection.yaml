apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dlsp.fullname" . }}-test-connection"
  labels:
    {{- include "dlsp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dlsp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
