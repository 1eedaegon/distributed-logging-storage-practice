apiVersion: v1
kind: Service
metadata:
  name: {{ include "dlsp.fullname" . }}
  labels:
    {{- include "dlsp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dlsp.selectorLabels" . | nindent 4 }}
